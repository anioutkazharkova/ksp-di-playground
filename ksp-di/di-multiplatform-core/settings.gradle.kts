pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
        mavenLocal()
    }
}
//includeBuild("convention-plugins")
rootProject.name = "di-multiplatform-core"

